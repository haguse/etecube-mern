import styled from "styled-components";

export const Wrapper = styled.div`
  margin-top: 1rem;
  .edit-delete-buttons {
    display: flex;
    font-size: 15px;
  }

  .button-area {
    display: flex;
    flex-direction: row-reverse;
    margin-bottom: 1.5rem;
  }
`;

export const CompaniesCRUD = styled.div``;
